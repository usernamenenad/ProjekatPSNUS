<Window x:Class="PL.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        xmlns:local="clr-namespace:PL"
        mc:Ignorable="d"
        Title="PL" Height="720" Width="1280" ResizeMode="CanResize" WindowStartupLocation="CenterScreen"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        TextElement.FontWeight="Regular"
        TextElement.FontSize="13"
        TextOptions.TextFormattingMode="Ideal"
        TextOptions.TextRenderingMode="Auto">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="3*"/>
            <ColumnDefinition Width="15*"/>
        </Grid.ColumnDefinitions>
        <Grid Grid.Column="0">
            <materialDesign:Card Margin="5">
                <UniformGrid Columns="1">
                    <UniformGrid.Resources>
                        <Style x:Key="button1" BasedOn="{StaticResource MaterialDesignFlatSecondaryButton}" TargetType="Button">
                            <Setter Property="Margin" Value="25"/>
                            <Setter Property="Foreground" Value="#3d195b"/>
                        </Style>
                    </UniformGrid.Resources>
                    <Image Source="/Pictures/pllogo.png" HorizontalAlignment="Center" Width="190"/>
                    <Button Style="{StaticResource button1}">Dodaj tim</Button>
                    <Button Style="{StaticResource button1}">Dodaj igrača</Button>
                    <Button Style="{StaticResource button1}">Pretraži tim</Button>
                    <Button Style="{StaticResource button1}">Pretraži igrača</Button>
                    <Button Style="{StaticResource button1}">Uredi tim</Button>
                    <Button Style="{StaticResource button1}">Uredi igrača</Button>
                </UniformGrid>
            </materialDesign:Card>
        </Grid>
        <Grid Grid.Column="1">
            <materialDesign:Card Margin="5" Background="#F6F5F8">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="2*"/>
                        <ColumnDefinition Width="2*"/>
                    </Grid.ColumnDefinitions>
                    <Grid Grid.Column="0" Margin="10">
                        <DataGrid x:Name="TeamsDataGrid" 
                                  IsReadOnly="True" 
                                  AutoGenerateColumns="False"
                                  ItemsSource="{Binding Teams, UpdateSourceTrigger=PropertyChanged}"
                                  ScrollViewer.CanContentScroll="False"
                                  GridLinesVisibility="Horizontal"
                                  HorizontalGridLinesBrush="{DynamicResource DataGrid.Grid.HorizontalLineBrush}"
                                  HeadersVisibility="Column">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="Ime tima" Binding="{Binding Name}" Width="*"/>
                                <DataGridTextColumn Header="Lokacija" Binding="{Binding Location}" Width="*"/>
                                <DataGridTextColumn Header="Ime stadiona" Binding="{Binding StadiumName}" Width="*"/>
                                <DataGridTextColumn Header="Kapacitet stadiona" Binding="{Binding StadiumCapacity}" Width="*"/>
                            </DataGrid.Columns>
                        </DataGrid>
                    </Grid>
                    <Grid Grid.Column="1" Margin="10">
                        <DataGrid x:Name="PlayersDataGrid" IsReadOnly="True">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="Ime igrača" Binding="{Binding PlayerName}" Width="*"/>
                                <DataGridTextColumn Header="Tim" Binding="{Binding PlayerTeam}" Width="*"/>
                            </DataGrid.Columns>
                        </DataGrid>
                    </Grid>
                </Grid>
            </materialDesign:Card>
        </Grid>
    </Grid>
</Window>
